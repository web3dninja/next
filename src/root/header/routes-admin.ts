import { type RouteWithDropdown } from './routes';

// Admin routes based on folders in /admin
export const adminRoutes: RouteWithDropdown[] = [
  { href: '/admin/users', label: 'Users' },
  // Add more routes here as you create new folders in /admin
];
